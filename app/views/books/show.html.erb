 <% if flash[:notice] %>
   <p id="notice">
    <font color="green">
       <%= flash[:notice] %>
    </font>
   </p>
   <% end %>

<h2>Book detail</h2>
  <table>
    <tr>
      <td></td>
      <td><%= @book.title %></td>
      <td><%= @book.body %></td>
      <td><a class="Edit" href=<%= edit_book_path(@book.id) %>>Edit</a></td>

      <td>
      <%= link_to "destroy", book_path(@book), method: :delete,data: { confirm: '本当に消しますか？' } %>
      </td>
    </tr>
  </table>

<a class="back" href="/books">Back</a>